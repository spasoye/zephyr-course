CONFIG_GPIO=y

CONFIG_LED_STRIP=y
CONFIG_SPI=y
CONFIG_WS2812_STRIP_SPI=y

# WTF: dont need everything for USB CDC ACM, but it is what it is
CONFIG_USB_CDC_ACM=y
CONFIG_SERIAL=y
CONFIG_STDOUT_CONSOLE=y
CONFIG_CONSOLE=y
CONFIG_UART_CONSOLE=y

CONFIG_LOG=y

# Iomico lecture 4 homework task dummy config
# LED Subsystem
CONFIG_LED_SUBSYSTEM=y

# Advanced LED features
CONFIG_ADV_LED=y
CONFIG_LED_BRIGHTNESS=100
CONFIG_LED_FADE_MS=500

CONFIG_BLINK_SLEEP_250MS=y

# openocd
CONFIG_BUILD_OUTPUT_HEX=y