#include <zephyr/dt-bindings/led/led.h>

/{
    aliases {
        app-led = &led0;    
    };

    leds {
        compatible = "gpio-leds";
        led0: led_0 {
            gpios = <&gpio0 10 GPIO_ACTIVE_HIGH>;
            label = "Heartbeat LED";
        };
    };
};